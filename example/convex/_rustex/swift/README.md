# RustexGenerated

Generated Swift bindings for the Convex app `rustex`.

Import `RustexGenerated` from your app target and create a `RustexClient` with either a deployment URL or an existing `ConvexClient` / `ConvexClientWithAuth` instance.

```swift
import RustexGenerated

let client = RustexClient(deploymentUrl: deploymentUrl)
```
